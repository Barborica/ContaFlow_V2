# ContaFlow

An application that automates invoice and receipt collection, extracts data using OCR, and exports it to SAGA-compatible XML format.

## Tech Stack

- Backend: Python (FastAPI, SQLAlchemy)
- OCR: PaddleOCR
- Mobile: Expo / React Native
